<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="jimat">
	<select id="detailpage_all" parameterType="dto.DetailpageDTO"
		resultType="dto.DetailpageDTO">
		SELECT foodstore_id, foodtype, road_name, latitude, longitude,foodstroe_num,img_url
		FROM seoulfoodsample
	</select>
</mapper>